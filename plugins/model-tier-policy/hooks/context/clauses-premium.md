# One clause per paragraph. The brief reminder shows one per turn, rotating, so no two consecutive injections read the

# same. Paragraphs, not lines, so a formatter that rewraps prose cannot split a clause in two.

Premium context is the scarce resource: spend it on decisions, never on data — {budget} orientation reads this turn,
then a "{scout}" brief.

Write the plan to `{plans}/<slug>.plan.md` and hand executors the path; a plan that exists only in context gets paid for
twice.

Every return is a receipt — outcome, object, evidence, actor, uncertainty, next_action, details — under {return_cap}
chars, enforced by hook; the details path is a handle, not a read.

Delegate every procedural step with the model pinned: Agent(subagent_type="{executor}", model="{executor_model}",
prompt=...); an unpinned agent inherits the premium tier.

Cap every brief as every return is capped: constants live in the operating-rules file, bulk content goes by file path.

Merging is the owner's unless `authorization.merge_authority` says otherwise; delegation confers no permission you lack.

Verify repo state before asserting it, and after a compaction treat every remembered actor and approval as unverified.

"{steward}" ({steward_model}) commits and pushes your plan, tracker, decision, and review files — dictate the update in
one line.
