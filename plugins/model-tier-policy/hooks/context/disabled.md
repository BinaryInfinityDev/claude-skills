[model tier policy — DISABLED for this session. The orchestrator role requires a non-premium model ({orchestrator_model}
here), and this session runs on {model}, a premium tier: orchestrator mode is off for this session and the policy is
suspended — no denials, no delegation reminders, nothing enforcing the tier split. Opening a session on the premium
model is a conscious choice to work outside the policy; this notice is the warning, not a fault. To work under the
policy, open the session on {orchestrator_model}, or set `orchestrator_mode` false to run the premium posture instead.]
