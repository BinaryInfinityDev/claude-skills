[model tier policy — orchestrator session ({model}): coordinate and dispatch; procedural tools are hook-denied, ticket
writes allowed. Status is a tracker-row edit plus a "{steward}" dispatch; no replies to no-op events. Full policy:
.claude/rules/model-tier-policy.md]
