[model tier policy — active, posture pending. No assistant turn is in the transcript yet, so the session's model — and
with it the posture: premium, orchestrator, worker, or disabled — is unknown until the first tool call; the full
reminder lands with the next prompt. Orchestrator mode is {orchestrator_state}. Whatever the posture resolves to, the
constants hold: plans go to `{plans}/`, procedural work is delegated with the model pinned
(Agent(subagent_type="{executor}", model="{executor_model}", ...)), and every brief and every return is capped. Full
policy: .claude/rules/model-tier-policy.md]
